package interfaces;

public interface Imprimivel {

    //Numa interface, os métodos são, por padrão, públicos e abstratos. Não precisamos colocar as palavras-chave "public" ou "abstract" aqui.

    void imprimir();
}